// Este archivo es opcional ahora, pero lo mantengo por si quieres agregar efectos.
// Actualmente las páginas son HTML separados, así que no necesitas lógica de pestañas.

// Pequeño efecto de fade adicional si quieres, pero el CSS ya maneja la animación principal.
document.addEventListener('DOMContentLoaded', () => {
    // peqieño log para confirmar carga
    console.log('Studio — cargado');
});